<div style='display:flex; justify-content:center'>
  <form
    id='addForm'
    action='/profile/add'
    method='post'
    enctype='multipart/form-data'
    {{#if admin}}
      style='width:60vh; margin:0px ;padding:0px; box-shadow: 2px 2px 50px 11px rgba(208, 244, 191, 0.571); position:fixed; z-index: 100; border-radius:10px'
    {{else}}
      style='width:60vh; margin:0px ;padding:0px; box-shadow: 2px 2px 50px 11px rgba(245, 176, 164, 0.536); position:fixed; z-index: 100; border-radius:10px'
    {{/if}}
  >
    <div 
    {{#if admin}}
      class='alert alert-success' 
    {{else}}
      class='alert alert-danger'
    {{/if}}
    role='alert' style='margin:0; text-align:center'>
      {{#if admin}}
      <b>{{user_name}}</b>
      стал админом!
      {{else}}
      <b>{{user_name}}</b>
      больше не Админ!
      {{/if}}
    </div>
  </form>
</div>
